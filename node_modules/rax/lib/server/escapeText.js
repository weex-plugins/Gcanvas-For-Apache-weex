'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = escapeText;
var ESCAPE_LOOKUP = {
  '&': '&amp;',
  '>': '&gt;',
  '<': '&lt;',
  '"': '&quot;',
  '\'': '&#x27;'
};

var ESCAPE_REGEX = /[&><"']/g;

function escaper(match) {
  return ESCAPE_LOOKUP[match];
}

function escapeText(text) {
  return String(text).replace(ESCAPE_REGEX, escaper);
}